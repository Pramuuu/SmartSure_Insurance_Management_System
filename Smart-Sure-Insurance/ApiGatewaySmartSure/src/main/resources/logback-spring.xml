<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <!-- Resolve LOKI_URL env var with a safe fallback -->
    <property name="LOKI_URL" value="http://localhost:3100/loki/api/v1/push" />
    <property name="LOG_FILE" value="/tmp/smartsure.log" />

    <!-- ========================================== -->
    <!-- LOKI APPENDER -->
    <!-- ========================================== -->
    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>${LOKI_URL}</url>
        </http>

        <format>
            <label>
                <pattern>app=smartsure,host=localhost,level=%level</pattern>
            </label>
            <message>
                <pattern>
                    %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg %X{traceId:-} %X{spanId:-}
                </pattern>
            </message>
            <sortByTime>true</sortByTime>
        </format>
    </appender>

    <!-- ========================================== -->
    <!-- ASYNC WRAPPER -->
    <!-- ========================================== -->
    <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="LOKI" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- ========================================== -->
    <!-- ROOT LOGGER -->
    <!-- ========================================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_LOKI" />
    </root>

    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.springframework.web" level="WARN"/>

</configuration>