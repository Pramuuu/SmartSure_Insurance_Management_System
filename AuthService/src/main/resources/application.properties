spring.application.name=authservice
server.port=8081

# ===============================
# DATABASE CONFIG (MySQL)
# Defined here as fallback when config server (port 8888) is unreachable.
# If config server is running, its values override these.
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/authservice_database?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=pramod280504
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE CONFIG
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true

internal.secret=JHGUY72638DEGWRT7D4365N89SOQL89234Y3IUTBR7DT4NQ3479387DNBYFGR366746823BETYX

# ===============================
# JWT CONFIG
# ===============================
jwt.secret=767BI36HSIUNYD7352198N1SV12BS62428748NDH7878BXB7B65D326B67SBVV6VS62545265776XBHG
jwt.expiration=86400000

# ===============================
# OBSERVABILITY - TRACING (Zipkin)
# Sampling set to 0.1 (10%) to reduce overhead.
# close-timeout set to 0 so Zipkin reporter does not block app shutdown.
# ===============================
management.tracing.sampling.probability=0.1
management.zipkin.tracing.endpoint=http://localhost:9411/api/v2/spans
spring.zipkin.sender.close-timeout=0
logging.pattern.level=%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]

# ===============================
# OBSERVABILITY - PROMETHEUS METRICS
# ===============================
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoints.web.base-path=/actuator
management.endpoint.prometheus.access=unrestricted
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# ===============================
# LOKI URL
# ===============================
loki.url=http://localhost:3100