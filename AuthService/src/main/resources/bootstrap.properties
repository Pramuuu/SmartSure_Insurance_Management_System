spring.application.name=authservice
# Config server disabled — all config is in application.properties.
# Previously fail-fast=true caused crash when config server was unreachable.
# Now fully disabled so bootstrap phase does not override actuator/management properties.
spring.cloud.config.enabled=false
spring.cloud.config.fail-fast=false